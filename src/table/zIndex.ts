export const Z_INDEX = {
  /** z-index of a regular cell. */
  CELL: 0,
  /** z-index of the active cell outline. */
  CELL_OUTLINE: 10,
  /** z-index of a row header. */
  ROW_HEADER: 20,
  /** z-index of the active row header outline. */
  ROW_OUTLINE: 30,
  /** z-index of the table header. */
  TABLE_HEADER: 40,
  /** z-index of the acitve column header outline. */
  COLUMN_OUTLINE: 50,
  /** z-index of the top-left box. */
  CORNER_BOX: 60,
  /** z-index of the new row button. */
  NEW_ROW: 0,
}
